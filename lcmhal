#!/usr/bin/env bash

# Simple wrapper so you can run:
#   lcmhal run <TESTCASE_DIR>
# and other main.py subcommands, without typing `python main.py`.

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

python "${SCRIPT_DIR}/main.py" "$@"

